<!DOCTYPE html>
<html>
<head>
    <title>Sửa Đặt chỗ</title>
</head>
<style>
    /* Reset */
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

/* Body Styling */
body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    padding: 20px;
    text-align: center;
}

h1 {
    margin-bottom: 20px;
    color: #333;
}

/* Form Container */
form {
    background-color: #fff;
    padding: 20px;
    width: 400px;
    margin: 0 auto;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    text-align: left;
}

/* Form Labels & Inputs */
label {
    display: block;
    margin-top: 12px;
    font-weight: bold;
    color: #444;
}

input[type="text"],
input[type="date"],
input[type="time"] {
    width: 100%;
    padding: 10px;
    margin-top: 5px;
    border: 1px solid #ccc;
    border-radius: 4px;
    transition: border-color 0.3s ease;
}

input[type="text"]:focus,
input[type="date"]:focus,
input[type="time"]:focus {
    border-color: #007BFF;
    outline: none;
}

/* Submit Button */
button[type="submit"] {
    width: 100%;
    background-color: #28a745; /* Xanh lá */
    color: white;
    padding: 12px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    margin-top: 20px;
    font-size: 16px;
    transition: background-color 0.3s ease;
}

button[type="submit"]:hover {
    background-color: #218838;
}

/* Back Link */
a {
    display: inline-block;
    margin-top: 20px;
    color: #007BFF;
    text-decoration: none;
    transition: color 0.3s ease;
}

a:hover {
    color: #0056b3;
}

/* Responsive */
@media (max-width: 480px) {
    form {
        width: 90%;
    }

    h1 {
        font-size: 20px;
    }

    button[type="submit"] {
        font-size: 14px;
    }
}
</style>
<body>
    <h1>Sửa Đặt chỗ</h1>
    <form action="/update/<%= booking.id %>" method="POST">
        <label>Tên Khách Hàng:</label><br>
        <input type="text" name="customerName" value="<%= booking.customerName %>" required><br>

        <label>Ngày:</label><br>
        <input type="date" name="date" value="<%= booking.date %>" required><br>

        <label>Giờ:</label><br>
        <input type="time" name="time" value="<%= booking.time %>" required><br><br>

        <button type="submit">Cập nhật</button>
    </form>
    <a href="/">Quay lại</a>
</body>
</html>